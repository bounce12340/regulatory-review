name: Regulatory Review — Test Suite

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: pytest (${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install core dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
          # Install core dependencies first
          pip install streamlit pandas numpy pyyaml plotly sqlalchemy bcrypt PyJWT
          pip install python-docx reportlab
          pip install requests urllib3 python-dotenv pytz rich

      - name: Run test suite (core only)
        env:
          REGULATORY_DB_PATH: ":memory:"
          PYTHONIOENCODING: utf-8
          SKIP_AI_TESTS: "1"
        run: |
          pytest tests/ \
            --tb=short \
            -v \
            --ignore=tests/test_ai.py \
            --cov=scripts \
            --cov=database \
            --cov=auth \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-fail-under=80

      - name: Upload coverage report
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.11'
        with:
          files: ./coverage.xml
          flags: unittests
          name: regulatory-review-coverage
          fail_ci_if_error: false

      - name: Upload HTML coverage artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-html-py${{ matrix.python-version }}
          path: htmlcov/
          retention-days: 14

  lint:
    name: Code quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install lint tools
        run: |
          pip install flake8 black

      - name: Run flake8
        run: |
          flake8 scripts/ database/ auth/ ai/ \
            --max-line-length=110 \
            --extend-ignore=E203,W503 \
            --exclude=__pycache__,tests/__pycache__

      - name: Check black formatting
        run: |
          black --check --line-length=110 scripts/ database/ auth/ ai/ || true
        continue-on-error: true
